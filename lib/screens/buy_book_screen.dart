import 'package:flutter/material.dart';

class BuyBookScreen extends StatelessWidget {
  const BuyBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("buy_book_screen"),
    ));
  }
}
